# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: system-config.spec.ts >> 系统配置页面 >> 品牌信息表单元素存在
- Location: tests/e2e/system-config.spec.ts:100:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('button:has-text("重置")')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('button:has-text("重置")')

```

```yaml
- text: DB DB-GPT
- img "appstore"
- text: 工作区
- img "message"
- text: 对话
- img "file-text"
- text: 我的报告
- img "star"
- text: 收藏夹 探索
- img "appstore"
- text: 模板广场
- img "team"
- text: 团队共享 配置中心
- img "database"
- text: 数据源
- img "book"
- text: 知识库 开发者中心
- img "tool"
- text: 技能
- img "edit"
- text: 提示词
- img "apartment"
- text: 工作流
- img "appstore"
- text: 应用管理
- img "database"
- text: 模型管理
- img "line-chart"
- text: 模型评测
- img "global"
- text: 社区 系统管理
- img "user-add"
- text: 审核用户
- img "user"
- text: 用户管理
- img "key"
- text: 角色管理
- img "apartment"
- text: 部门管理
- img "safety"
- text: 权限管理
- img "appstore"
- text: system_config k kk 对话
- img "question-circle"
- img "bell"
- button "plus 创建会话":
  - img "plus"
  - text: 创建会话
- text: 系统管理 / 系统配置
- heading "系统配置" [level=1]
- paragraph: 管理系统的基础参数、品牌信息和功能开关
- tablist:
  - tab "bg-colors 品牌信息" [selected]:
    - img "bg-colors"
    - text: 品牌信息
  - tab "dashboard 记忆管理":
    - img "dashboard"
    - text: 记忆管理
  - tab "lock 安全设置":
    - img "lock"
    - text: 安全设置
  - tab "bell 通知设置":
    - img "bell"
    - text: 通知设置
  - tab "sliders 高级设置":
    - img "sliders"
    - text: 高级设置
- tabpanel "bg-colors 品牌信息":
  - text: Logo 配置
  - paragraph: 当前 Logo
  - img "setting"
  - paragraph: 上传新 Logo
  - button "plus 点击或拖拽图片到此处 支持 SVG、PNG、JPG，建议尺寸 64×64px":
    - paragraph:
      - img "plus"
    - paragraph: 点击或拖拽图片到此处
    - paragraph: 支持 SVG、PNG、JPG，建议尺寸 64×64px
  - text: 产品名称 中文名称
  - textbox "显示在导航栏、登录页、浏览器标题": DB-GPT
  - paragraph: 显示在导航栏、登录页、浏览器标题
  - text: 英文名称
  - textbox "用于 API 响应、系统日志等场景": DB-GPT
  - paragraph: 用于 API 响应、系统日志等场景
  - text: 系统标语 标语内容
  - textbox "建议不超过 20 个字符": 开口问数，预见洞察
  - paragraph: 建议不超过 20 个字符，显示在对话首页中央
- text: 暂无未保存的修改
- button "重 置" [disabled]
- button "保存配置"
- alert: 系统配置
```

# Test source

```ts
  25  |     const url = page.url();
  26  |     console.log('After login, current URL:', url);
  27  | 
  28  |     // 检查是否仍在登录页（登录失败）
  29  |     if (url.includes('/login')) {
  30  |       // 查找错误消息
  31  |       const errorMsg = await page.locator('.ant-alert-error').textContent().catch(() => 'No error message found');
  32  |       console.log('Login failed, error:', errorMsg);
  33  |     }
  34  |   });
  35  | 
  36  |   test('访问系统配置页面', async ({ page }) => {
  37  |     // 直接导航到系统配置页面
  38  |     await page.goto(`${BASE_URL}/`);
  39  |     await page.waitForLoadState('networkidle');
  40  |     await page.waitForTimeout(2000);
  41  | 
  42  |     // 使用 Next.js router 导航到系统配置页面
  43  |     await page.evaluate(() => {
  44  |       const next = (window as any).next;
  45  |       if (next && next.router) {
  46  |         next.router.push('/admin/system-config');
  47  |       }
  48  |     });
  49  | 
  50  |     await page.waitForLoadState('networkidle');
  51  |     await page.waitForTimeout(3000);
  52  | 
  53  |     // Debug: 打印页面内容
  54  |     console.log('Current URL:', page.url());
  55  |     const content = await page.content();
  56  |     console.log('Page contains 系统管理:', content.includes('系统管理'));
  57  |     console.log('Page contains 系统配置:', content.includes('系统配置'));
  58  | 
  59  |     // 验证页面存在 - 检查面包屑或页面标题
  60  |     await expect(page.locator('text=系统配置').first()).toBeVisible({ timeout: 15000 });
  61  |   });
  62  | 
  63  |   test('5个Tab切换正常', async ({ page }) => {
  64  |     await page.goto(`${BASE_URL}/`);
  65  |     await page.waitForLoadState('networkidle');
  66  |     await page.waitForTimeout(2000);
  67  | 
  68  |     // 使用 Next.js router 导航
  69  |     await page.evaluate(() => {
  70  |       const next = (window as any).next;
  71  |       if (next && next.router) {
  72  |         next.router.push('/admin/system-config');
  73  |       }
  74  |     });
  75  | 
  76  |     await page.waitForLoadState('networkidle');
  77  |     await page.waitForTimeout(3000);
  78  | 
  79  |     // 切换到记忆管理 Tab
  80  |     await page.click('.ant-tabs-tab:has-text("记忆管理")');
  81  |     await expect(page.locator('text=总记忆条数')).toBeVisible({ timeout: 5000 });
  82  | 
  83  |     // 切换到安全设置 Tab
  84  |     await page.click('.ant-tabs-tab:has-text("安全设置")');
  85  |     await expect(page.locator('text=密码策略')).toBeVisible({ timeout: 5000 });
  86  | 
  87  |     // 切换到通知设置 Tab
  88  |     await page.click('.ant-tabs-tab:has-text("通知设置")');
  89  |     await expect(page.locator('text=邮件通知')).toBeVisible({ timeout: 5000 });
  90  | 
  91  |     // 切换到高级设置 Tab
  92  |     await page.click('.ant-tabs-tab:has-text("高级设置")');
  93  |     await expect(page.locator('text=系统维护')).toBeVisible({ timeout: 5000 });
  94  | 
  95  |     // 切回品牌信息 Tab
  96  |     await page.click('.ant-tabs-tab:has-text("品牌信息")');
  97  |     await expect(page.locator('text=Logo 配置')).toBeVisible({ timeout: 5000 });
  98  |   });
  99  | 
  100 |   test('品牌信息表单元素存在', async ({ page }) => {
  101 |     await page.goto(`${BASE_URL}/`);
  102 |     await page.waitForLoadState('networkidle');
  103 |     await page.waitForTimeout(2000);
  104 | 
  105 |     // 使用 Next.js router 导航
  106 |     await page.evaluate(() => {
  107 |       const next = (window as any).next;
  108 |       if (next && next.router) {
  109 |         next.router.push('/admin/system-config');
  110 |       }
  111 |     });
  112 | 
  113 |     await page.waitForLoadState('networkidle');
  114 |     await page.waitForTimeout(3000);
  115 | 
  116 |     // 验证当前 Logo 预览区域存在
  117 |     await expect(page.locator('text=当前 Logo')).toBeVisible({ timeout: 10000 });
  118 |     await expect(page.locator('text=上传新 Logo')).toBeVisible({ timeout: 5000 });
  119 | 
  120 |     // 验证产品名称字段
  121 |     await expect(page.locator('text=中文名称')).toBeVisible({ timeout: 5000 });
  122 |     await expect(page.locator('text=英文名称')).toBeVisible({ timeout: 5000 });
  123 | 
  124 |     // 验证底部操作栏
> 125 |     await expect(page.locator('button:has-text("重置")')).toBeVisible({ timeout: 5000 });
      |                                                         ^ Error: expect(locator).toBeVisible() failed
  126 |     await expect(page.locator('button:has-text("保存配置")')).toBeVisible({ timeout: 5000 });
  127 |   });
  128 | 
  129 |   test('记忆管理Tab显示统计卡片', async ({ page }) => {
  130 |     await page.goto(`${BASE_URL}/`);
  131 |     await page.waitForLoadState('networkidle');
  132 |     await page.waitForTimeout(2000);
  133 | 
  134 |     // 使用 Next.js router 导航
  135 |     await page.evaluate(() => {
  136 |       const next = (window as any).next;
  137 |       if (next && next.router) {
  138 |         next.router.push('/admin/system-config');
  139 |       }
  140 |     });
  141 | 
  142 |     await page.waitForLoadState('networkidle');
  143 |     await page.waitForTimeout(3000);
  144 | 
  145 |     // 切换到记忆管理
  146 |     await page.click('.ant-tabs-tab:has-text("记忆管理")');
  147 | 
  148 |     // 验证4个统计卡片
  149 |     await expect(page.locator('text=总记忆条数')).toBeVisible({ timeout: 5000 });
  150 |     await expect(page.locator('text=今日新增')).toBeVisible({ timeout: 5000 });
  151 |     await expect(page.locator('text=覆盖用户数')).toBeVisible({ timeout: 5000 });
  152 |     await expect(page.locator('text=准确率')).toBeVisible({ timeout: 5000 });
  153 | 
  154 |     // 验证蒸馏策略
  155 |     await expect(page.locator('text=蒸馏策略')).toBeVisible({ timeout: 5000 });
  156 | 
  157 |     // 验证操作按钮
  158 |     await expect(page.locator('button:has-text("清除全部记忆")')).toBeVisible({ timeout: 5000 });
  159 |   });
  160 | 
  161 |   test('安全设置Tab配置项', async ({ page }) => {
  162 |     await page.goto(`${BASE_URL}/`);
  163 |     await page.waitForLoadState('networkidle');
  164 |     await page.waitForTimeout(2000);
  165 | 
  166 |     // 使用 Next.js router 导航
  167 |     await page.evaluate(() => {
  168 |       const next = (window as any).next;
  169 |       if (next && next.router) {
  170 |         next.router.push('/admin/system-config');
  171 |       }
  172 |     });
  173 | 
  174 |     await page.waitForLoadState('networkidle');
  175 |     await page.waitForTimeout(3000);
  176 | 
  177 |     // 切换到安全设置
  178 |     await page.click('.ant-tabs-tab:has-text("安全设置")');
  179 | 
  180 |     // 验证密码策略
  181 |     await expect(page.locator('text=密码策略')).toBeVisible({ timeout: 5000 });
  182 |     await expect(page.locator('text=密码最小长度')).toBeVisible({ timeout: 5000 });
  183 | 
  184 |     // 验证会话安全
  185 |     await expect(page.locator('text=会话安全')).toBeVisible({ timeout: 5000 });
  186 | 
  187 |     // 验证数据安全
  188 |     await expect(page.locator('text=数据安全')).toBeVisible({ timeout: 5000 });
  189 |   });
  190 | 
  191 |   test('通知设置Tab表单', async ({ page }) => {
  192 |     await page.goto(`${BASE_URL}/`);
  193 |     await page.waitForLoadState('networkidle');
  194 |     await page.waitForTimeout(2000);
  195 | 
  196 |     // 使用 Next.js router 导航
  197 |     await page.evaluate(() => {
  198 |       const next = (window as any).next;
  199 |       if (next && next.router) {
  200 |         next.router.push('/admin/system-config');
  201 |       }
  202 |     });
  203 | 
  204 |     await page.waitForLoadState('networkidle');
  205 |     await page.waitForTimeout(3000);
  206 | 
  207 |     // 切换到通知设置
  208 |     await page.click('.ant-tabs-tab:has-text("通知设置")');
  209 | 
  210 |     // 验证SMTP配置
  211 |     await expect(page.locator('text=邮件通知')).toBeVisible({ timeout: 5000 });
  212 |     await expect(page.locator('text=SMTP 服务器')).toBeVisible({ timeout: 5000 });
  213 | 
  214 |     // 验证通知场景
  215 |     await expect(page.locator('text=通知场景')).toBeVisible({ timeout: 5000 });
  216 |   });
  217 | 
  218 |   test('高级设置Tab系统信息', async ({ page }) => {
  219 |     await page.goto(`${BASE_URL}/`);
  220 |     await page.waitForLoadState('networkidle');
  221 |     await page.waitForTimeout(2000);
  222 | 
  223 |     // 使用 Next.js router 导航
  224 |     await page.evaluate(() => {
  225 |       const next = (window as any).next;
```